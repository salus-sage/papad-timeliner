function editFileContents(files, file, newContents) {
  if (!files[file]) {
    files[file] = {};
  }
  files[file].contents = newContents;
  return files[file];
}

module.exports = editFileContents;
