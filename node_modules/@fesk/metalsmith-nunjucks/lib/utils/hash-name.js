const crypto = require('crypto');
const path = require('path');

function hashName(name, key = '') {
  const hash = crypto
    .createHash('md5')
    .update(key || name)
    .digest('hex');

  return `_compiled/${hash}/${path.basename(name)}`;
}

module.exports = hashName;
