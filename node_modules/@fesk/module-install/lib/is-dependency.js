const cachedPackageJson = require('./cached-package-json');

function isDependency(packageName, customPackageJson) {
  const packageJson = customPackageJson || cachedPackageJson();

  return (
    packageJson.dependencies &&
    Object.keys(packageJson.dependencies).indexOf(packageName)
  );
}

module.exports = isDependency;
