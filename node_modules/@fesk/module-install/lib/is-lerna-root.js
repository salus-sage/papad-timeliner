const fs = require('fs');
const path = require('path');

function isLernaRoot(packagePath) {
  const cwd = packagePath || process.cwd();
  return fs.existsSync(path.resolve(path.join(cwd, 'lerna.json')));
}

module.exports = isLernaRoot;
