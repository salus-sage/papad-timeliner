const getLernaPackages = require('./get-lerna-packages');

async function isLocalPackage(packageName, packagePath) {
  const cwd = packagePath || process.cwd();
  const packages = await getLernaPackages(cwd);
  return packages.indexOf(packageName) !== -1;
}

module.exports = isLocalPackage;
