const wrapConfig = require('./wrapConfig');

module.exports = (makeConfig, opts) =>
  new Promise((resolve, error) =>
    wrapConfig(makeConfig, opts).build(err => (err ? error(err) : resolve()))
  );
