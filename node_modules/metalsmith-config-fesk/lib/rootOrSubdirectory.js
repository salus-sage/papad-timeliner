function rootOrSubdirectory(isRoot, defaultPath) {
  return files => {
    Object.keys(files).forEach(key => {
      files[key].pagePrefix = isRoot ? '' : `/${defaultPath}`;
    });
  };
}

module.exports = rootOrSubdirectory;
